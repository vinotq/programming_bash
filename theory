#. - sym
#\d - one num
#\D - all, only no num
#\s - empty or space
#\S - all, only space no
#\w - all letters, num and _
#\W - all, only no letters, num and _
#[..] - [0-9][0-9A-Fa-f] - for validation 16-b system
#[^..] - all, only no ^ sym (can be choosed)
#\b - start or end of word
#\B - word in other word

#{x} - x-how mny times /d (for ex) need to be to be true
#{x,} - no less than x times
#{,x} - no more than x times